{
  "name": "llm-grammar-checker-demo-public",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run build:css && bun run build.ts && bun run src/server.ts",
    "build": "bun run build:css && bun run build:js",
    "build:css": "bunx @tailwindcss/cli -i src/index.css -o dist/index.css --minify",
    "build:js": "bun run build.ts"
  },
  "dependencies": {
    "@heroui/react": "^3.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "bun-types": "^1.4.0"
  }
}
